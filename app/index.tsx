import { View } from "react-native";

export default function Index() {
  // Ce composant est vide car toute la logique de redirection
  // est centralisée dans le fichier ./_layout.tsx
  return <View style={{ flex: 1, backgroundColor: '#F5F0EB' }} />;
}
